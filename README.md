# Server — Zeus

Código e ferramentas do **servidor** do projeto Zeus ficam **a partir desta pasta** (`Server/`).

## Conteúdo atual

| Pasta | Descrição |
|--------|------------|
| [`ZeusServerEngine/`](ZeusServerEngine/README.md) | Motor do servidor C++20 (VS2022 + CMake Linux), Parte 1 da fundação |

Novos serviços (ex.: `MapServer`, ferramentas CLI) podem ser adicionados como pastas irmãs de `ZeusServerEngine/`.

## Repositório remoto

O histórico Git desta árvore está ligado a: `https://github.com/Mateuus/ZeusServerEngine.git` (raiz do clone = `Server/`).
